import * as Lark from '@larksuiteoapi/node-sdk';

// Check the register method signature
console.log('EventDispatcher.register signature:', Lark.EventDispatcher.prototype.register.toString().substring(0, 500));
